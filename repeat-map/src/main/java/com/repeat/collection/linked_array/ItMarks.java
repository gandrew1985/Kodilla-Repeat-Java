package com.repeat.collection.linked_array;

public class ItMarks {

    /*public static void main(String[] args) {

        List<Double> itMarks = new ArrayList<>();

        itMarks.add(3.0);
        itMarks.add(4.0);
        itMarks.add(2.0);
        itMarks.add(5.0);
        itMarks.add(3.5);

        for (Double mark : itMarks) {
            System.out.println(mark);
        }

        itMarks.remove(Collections.max(itMarks));
        itMarks.remove(Collections.min(itMarks));

        double sum = 0;

        for (double mark : itMarks) {
            sum += mark;
        }
        double avg = sum / itMarks.size();
        System.out.println("\n Summary: " + sum + "\n Average: " + avg);


        List<Book> bookList = new LinkedList<>();

        for (int i = 0; i < 10; i++) {
            bookList.add(new Book("title" + i, 2000 + i));
        }

        for (Book book : bookList) {
            if(book.getYear() > 2004) {
                System.out.println(book);
            };
        }
    }*/
}
